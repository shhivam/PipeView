---
phase: 02-menu-bar-display
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - BandwidthMonitor/MenuBar/SpeedFormatter.swift
  - BandwidthMonitor/MenuBar/SpeedTextBuilder.swift
  - BandwidthMonitorTests/SpeedFormatterTests.swift
  - BandwidthMonitorTests/SpeedTextBuilderTests.swift
  - BandwidthMonitor.xcodeproj/project.pbxproj
autonomous: true
requirements: [BAR-01, BAR-02, BAR-03]

must_haves:
  truths:
    - "SpeedFormatter converts bytes/sec to correct SI-unit strings with adaptive precision"
    - "SpeedFormatter handles all UnitMode cases (auto, fixedKB, fixedMB, fixedGB) with ceiling fallback"
    - "SpeedTextBuilder composes correct display strings for all four DisplayMode cases (auto, uploadOnly, downloadOnly, both)"
    - "Zero/near-zero traffic (< 1 KB/s) always produces '0 KB/s'"
    - "No active interfaces produces em dash (U+2014)"
    - "Auto mode shows the direction with higher traffic, upload wins ties"
  artifacts:
    - path: "BandwidthMonitor/MenuBar/SpeedFormatter.swift"
      provides: "Speed formatting with SI units, adaptive precision, unit modes"
      exports: ["SpeedFormatter", "SpeedFormatter.UnitMode"]
      contains: "func format(bytesPerSecond"
    - path: "BandwidthMonitor/MenuBar/SpeedTextBuilder.swift"
      provides: "Display string composition from Speed + DisplayMode + UnitMode"
      exports: ["SpeedTextBuilder", "DisplayMode"]
      contains: "func build("
    - path: "BandwidthMonitorTests/SpeedFormatterTests.swift"
      provides: "Unit tests for all formatting rules"
      min_lines: 40
    - path: "BandwidthMonitorTests/SpeedTextBuilderTests.swift"
      provides: "Unit tests for all display modes and edge cases"
      min_lines: 40
  key_links:
    - from: "BandwidthMonitor/MenuBar/SpeedTextBuilder.swift"
      to: "BandwidthMonitor/MenuBar/SpeedFormatter.swift"
      via: "SpeedTextBuilder uses SpeedFormatter.format()"
      pattern: "formatter\\.format\\("
    - from: "BandwidthMonitor/MenuBar/SpeedTextBuilder.swift"
      to: "BandwidthMonitor/Monitoring/NetworkSample.swift"
      via: "SpeedTextBuilder consumes Speed struct"
      pattern: "speed\\.bytesInPerSecond|speed\\.bytesOutPerSecond"
---

<objective>
Build and test the speed formatting engine that converts raw byte-rate data from NetworkMonitor into human-readable menu bar text.

Purpose: This is the pure-logic core of Phase 2 -- the SpeedFormatter and SpeedTextBuilder are used by StatusBarController (Plan 02) to render text in the menu bar. TDD ensures all 18 formatting decisions (D-01 through D-11, D-18) produce correct output before wiring to AppKit.

Output: Two tested Swift structs (SpeedFormatter, SpeedTextBuilder) and two XCTest files covering all formatting rules, display modes, and edge cases.
</objective>

<execution_context>
@/Users/shivamsinghal/projects/bandwidth-monitor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shivamsinghal/projects/bandwidth-monitor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-menu-bar-display/02-CONTEXT.md
@.planning/phases/02-menu-bar-display/02-RESEARCH.md
@.planning/phases/02-menu-bar-display/02-UI-SPEC.md

<interfaces>
<!-- Key types from Phase 1 that SpeedTextBuilder consumes -->

From BandwidthMonitor/Monitoring/NetworkSample.swift:
```swift
struct Speed: Equatable, Sendable {
    let bytesInPerSecond: Double   // download
    let bytesOutPerSecond: Double  // upload

    static let zero = Speed(bytesInPerSecond: 0, bytesOutPerSecond: 0)

    static func + (lhs: Speed, rhs: Speed) -> Speed {
        Speed(
            bytesInPerSecond: lhs.bytesInPerSecond + rhs.bytesInPerSecond,
            bytesOutPerSecond: lhs.bytesOutPerSecond + rhs.bytesOutPerSecond
        )
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SpeedFormatter and SpeedTextBuilder with TDD</name>
  <files>
    BandwidthMonitor/MenuBar/SpeedFormatter.swift,
    BandwidthMonitor/MenuBar/SpeedTextBuilder.swift,
    BandwidthMonitorTests/SpeedFormatterTests.swift,
    BandwidthMonitorTests/SpeedTextBuilderTests.swift,
    BandwidthMonitor.xcodeproj/project.pbxproj
  </files>
  <read_first>
    BandwidthMonitor/Monitoring/NetworkSample.swift,
    BandwidthMonitor.xcodeproj/project.pbxproj,
    .planning/phases/02-menu-bar-display/02-CONTEXT.md,
    .planning/phases/02-menu-bar-display/02-RESEARCH.md
  </read_first>
  <behavior>
    SpeedFormatterTests:
    - format(0) returns "0 KB/s" (D-10: below 1 KB/s threshold)
    - format(500) returns "0 KB/s" (D-10: 500 B/s is below 1,000 B/s threshold)
    - format(999) returns "0 KB/s" (D-10: just below 1 KB/s boundary)
    - format(1_000) returns "1.0 KB/s" (D-09: exactly at KB/s boundary; D-02: 1 decimal < 100)
    - format(1_200) returns "1.2 KB/s" (D-02: 1 decimal place)
    - format(45_300) returns "45.3 KB/s" (D-02: 1 decimal place)
    - format(99_900) returns "99.9 KB/s" (D-02: 1 decimal, just below 100)
    - format(100_000) returns "100 KB/s" (D-02: no decimal at 100+)
    - format(456_000) returns "456 KB/s" (D-02: no decimal at 100+)
    - format(999_999) returns "1000 KB/s" (D-09: still KB/s, just below MB boundary)
    - format(1_000_000) returns "1.0 MB/s" (D-09: MB/s boundary)
    - format(1_200_000) returns "1.2 MB/s" (D-02 + D-09)
    - format(100_000_000) returns "100 MB/s" (D-02: no decimal >= 100)
    - format(1_000_000_000) returns "1.0 GB/s" (D-09: GB/s boundary)
    - format(2_500_000_000) returns "2.5 GB/s" (D-02 + D-09)
    - format(1_200, unit: .fixedMB) returns "1.2 KB/s" (D-11: fixedMB is ceiling, 1200 B/s too small for MB, falls back to KB/s)
    - format(1_500_000, unit: .fixedMB) returns "1.5 MB/s" (D-11: within fixedMB range)
    - format(1_500_000, unit: .fixedKB) returns "1500 KB/s" (D-11: fixedKB ceiling, stays in KB)
    - format(1_000_000_000, unit: .fixedMB) returns "1000 MB/s" (D-11: fixedMB ceiling, cannot promote to GB)
    - format(1_000_000_000, unit: .fixedGB) returns "1.0 GB/s" (fixedGB allows GB)

    SpeedTextBuilderTests:
    - auto mode, download higher: Speed(bytesIn: 1_200_000, bytesOut: 500) returns "\u{2193} 1.2 MB/s" (D-06)
    - auto mode, upload higher: Speed(bytesIn: 500, bytesOut: 1_200_000) returns "\u{2191} 1.2 MB/s" (D-06)
    - auto mode, equal (both zero): Speed.zero returns "\u{2191} 0 KB/s" (D-06: upload wins tie)
    - auto mode, equal (both non-zero): Speed(bytesIn: 1_000, bytesOut: 1_000) returns "\u{2191} 1.0 KB/s" (D-06: upload wins tie, >= comparison)
    - upload only mode: returns "\u{2191} {formatted}" (D-07)
    - download only mode: returns "\u{2193} {formatted}" (D-07)
    - both mode: returns "\u{2191} {up} \u{2193} {down}" with space separator (D-08)
    - no interfaces: returns "\u{2014}" (em dash) regardless of speed values (D-18)
    - no interfaces in all modes: returns "\u{2014}" for auto, uploadOnly, downloadOnly, both (D-18)
  </behavior>
  <action>
    Follow TDD red-green-refactor cycle.

    **RED phase -- write failing tests first:**

    1. Create directory `BandwidthMonitor/MenuBar/` (new directory for Phase 2 code).

    2. Create `BandwidthMonitor/MenuBar/SpeedFormatter.swift` as a minimal stub:
       ```swift
       import Foundation

       struct SpeedFormatter: Sendable {
           enum UnitMode: Sendable {
               case auto
               case fixedKB
               case fixedMB
               case fixedGB
           }

           func format(bytesPerSecond: Double, unit: UnitMode = .auto) -> String {
               fatalError("Not implemented")
           }
       }
       ```

    3. Create `BandwidthMonitor/MenuBar/SpeedTextBuilder.swift` as a minimal stub:
       ```swift
       import Foundation

       enum DisplayMode: Sendable {
           case auto
           case uploadOnly
           case downloadOnly
           case both
       }

       struct SpeedTextBuilder: Sendable {
           let formatter: SpeedFormatter

           init(formatter: SpeedFormatter = SpeedFormatter()) {
               self.formatter = formatter
           }

           func build(
               speed: Speed,
               mode: DisplayMode = .auto,
               unit: SpeedFormatter.UnitMode = .auto,
               hasInterfaces: Bool
           ) -> String {
               fatalError("Not implemented")
           }
       }
       ```

    4. Create `BandwidthMonitorTests/SpeedFormatterTests.swift` with all test cases from the behavior block above. Import `@testable import BandwidthMonitor`. Each test should call `SpeedFormatter().format(bytesPerSecond:unit:)` and `XCTAssertEqual` against the expected string.

    5. Create `BandwidthMonitorTests/SpeedTextBuilderTests.swift` with all test cases from the behavior block above. Create `Speed` instances directly (the struct is in the same module). Each test should call `SpeedTextBuilder().build(speed:mode:unit:hasInterfaces:)` and `XCTAssertEqual` against the expected string.

    6. Add all 4 new Swift files to the Xcode project (`project.pbxproj`):
       - `SpeedFormatter.swift` and `SpeedTextBuilder.swift` in the main target's `BandwidthMonitor/MenuBar` group
       - `SpeedFormatterTests.swift` and `SpeedTextBuilderTests.swift` in the test target

    7. Run tests -- they MUST fail (stubs throw fatalError). Commit: `test(02-01): add failing tests for SpeedFormatter and SpeedTextBuilder`

    **GREEN phase -- implement to pass tests:**

    8. Implement `SpeedFormatter.format(bytesPerSecond:unit:)`:
       - D-10: If `bytesPerSecond < 1_000`, return `"0 KB/s"`.
       - D-09 + D-11: Call a private `selectUnit(bytesPerSecond:mode:)` method that returns `(value: Double, suffix: String)`:
         - `.auto`: >= 1B -> KB/s, >= 1M -> MB/s, >= 1G -> GB/s
         - `.fixedGB`: same as auto (GB is max anyway)
         - `.fixedMB`: ceiling at MB/s -- if >= 1M use MB/s, else KB/s. Cannot promote to GB/s even if >= 1G.
         - `.fixedKB`: always KB/s regardless of magnitude.
       - D-02: Call a private `formatValue(_ value: Double)` method:
         - If `value < 100`: `String(format: "%.1f", value)` (1 decimal)
         - If `value >= 100`: `String(format: "%.0f", value)` (0 decimals)
       - Return `"\(formatValue(value)) \(suffix)"`

    9. Implement `SpeedTextBuilder.build(speed:mode:unit:hasInterfaces:)`:
       - D-18: If `!hasInterfaces`, return `"\u{2014}"` (em dash).
       - D-06 `.auto`: If `speed.bytesOutPerSecond >= speed.bytesInPerSecond` (upload wins ties), return `"\u{2191} \(formatter.format(bytesPerSecond: speed.bytesOutPerSecond, unit: unit))"`. Else return `"\u{2193} \(formatter.format(bytesPerSecond: speed.bytesInPerSecond, unit: unit))"`.
       - D-07 `.uploadOnly`: `"\u{2191} \(formatter.format(bytesPerSecond: speed.bytesOutPerSecond, unit: unit))"`.
       - D-07 `.downloadOnly`: `"\u{2193} \(formatter.format(bytesPerSecond: speed.bytesInPerSecond, unit: unit))"`.
       - D-08 `.both`: `"\u{2191} \(up) \u{2193} \(down)"` where up and down are formatted independently.

    10. Run all tests -- they MUST pass. Commit: `feat(02-01): implement SpeedFormatter and SpeedTextBuilder`

    **REFACTOR phase (if needed):** Clean up, ensure Sendable conformance on both structs and the DisplayMode enum. Run tests again -- MUST still pass. Commit only if changes were made.

    IMPORTANT: Both `SpeedFormatter` and `DisplayMode`/`SpeedTextBuilder` must be in the BandwidthMonitor main target (not test target). The `Speed` struct is already accessible because it's in `NetworkSample.swift` in the same target.

    IMPORTANT: `format(1_000_000_000, unit: .fixedMB)` must return `"1000 MB/s"` not `"1.0 GB/s"` -- the fixedMB ceiling prevents promotion to GB. The `selectUnit` method for `.fixedMB` should be: if >= 1_000_000 return (bytesPerSecond / 1_000_000, "MB/s"), else return (bytesPerSecond / 1_000, "KB/s"). No GB branch.

    IMPORTANT: For `.fixedKB`, `format(1_500_000, unit: .fixedKB)` returns `"1500 KB/s"` -- value is 1500 which is >= 100, so 0 decimal places per D-02.
  </action>
  <verify>
    <automated>xcodebuild test -project BandwidthMonitor.xcodeproj -scheme BandwidthMonitor -only-testing BandwidthMonitorTests/SpeedFormatterTests -only-testing BandwidthMonitorTests/SpeedTextBuilderTests -quiet 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - BandwidthMonitor/MenuBar/SpeedFormatter.swift exists and contains `struct SpeedFormatter` and `enum UnitMode`
    - BandwidthMonitor/MenuBar/SpeedTextBuilder.swift exists and contains `struct SpeedTextBuilder` and `enum DisplayMode`
    - SpeedFormatter.swift contains `func format(bytesPerSecond: Double, unit: UnitMode = .auto) -> String`
    - SpeedTextBuilder.swift contains `func build(speed: Speed, mode: DisplayMode = .auto, unit: SpeedFormatter.UnitMode = .auto, hasInterfaces: Bool) -> String`
    - SpeedTextBuilder.swift contains `\u{2014}` (em dash for D-18)
    - SpeedTextBuilder.swift contains `\u{2191}` (up arrow for D-01) and `\u{2193}` (down arrow for D-01)
    - SpeedFormatterTests.swift contains at least 15 test methods covering all boundary values
    - SpeedTextBuilderTests.swift contains at least 8 test methods covering all display modes
    - `xcodebuild test -only-testing BandwidthMonitorTests/SpeedFormatterTests -only-testing BandwidthMonitorTests/SpeedTextBuilderTests` exits 0
    - All 29 pre-existing tests from Phase 1 still pass (no regressions)
  </acceptance_criteria>
  <done>
    SpeedFormatter correctly formats all byte-rate values per D-02, D-03, D-09, D-10, D-11 across all UnitMode cases. SpeedTextBuilder composes correct display strings for all four DisplayMode cases per D-01, D-06, D-07, D-08, D-18. All new tests pass. All Phase 1 tests still pass.
  </done>
</task>

</tasks>

<verification>
- All SpeedFormatter boundary values produce expected output
- All SpeedTextBuilder display modes produce expected output
- Zero traffic shows "0 KB/s", not "0 B/s" or blank
- Em dash shown for no-interface state
- Auto mode shows upload arrow when upload >= download (upload wins ties)
- Fixed unit modes respect ceiling behavior (D-11)
- All 29 Phase 1 tests still pass (no regressions)
</verification>

<success_criteria>
- SpeedFormatter and SpeedTextBuilder are fully tested with 20+ test cases
- All formatting decisions (D-01 through D-11, D-18) have at least one test
- Both structs are Sendable
- Full test suite passes: `xcodebuild test -scheme BandwidthMonitor -quiet` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-menu-bar-display/02-01-SUMMARY.md`
</output>
