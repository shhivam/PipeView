---
phase: 07-verification-and-artifact-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-popover-shell-and-interface-views/04-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [POP-01, POP-03, POP-05, POP-06]
must_haves:
  truths:
    - "Phase 4 has a VERIFICATION.md documenting verification of POP-01, POP-03, POP-05, POP-06"
    - "REQUIREMENTS.md checkboxes for BAR-01, BAR-02, BAR-03, BAR-04, POP-01, POP-03, POP-05, POP-06, SYS-01 are all checked [x]"
    - "ROADMAP.md phase list checkboxes for Phase 1, 2, 3, 6 are all checked [x]"
    - "ROADMAP.md progress table shows Phase 6 as Complete with 1/1 plans"
    - "REQUIREMENTS.md traceability table shows POP-01, POP-03, POP-05, POP-06 as Complete"
    - "REQUIREMENTS.md coverage line reflects the correct count of satisfied requirements"
  artifacts:
    - path: ".planning/phases/04-popover-shell-and-interface-views/04-VERIFICATION.md"
      provides: "Phase 4 verification report"
      contains: "Phase 4"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected requirement checkboxes"
      contains: "[x] **POP-01**"
    - path: ".planning/ROADMAP.md"
      provides: "Corrected phase checkboxes and progress table"
      contains: "[x] **Phase 6"
  key_links:
    - from: ".planning/phases/04-popover-shell-and-interface-views/04-VERIFICATION.md"
      to: ".planning/phases/04-popover-shell-and-interface-views/04-01-SUMMARY.md"
      via: "Evidence references from SUMMARY commits and file listings"
      pattern: "VERIFIED"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "Requirement status matches roadmap phase completion status"
      pattern: "Complete"
---

<objective>
Create Phase 4 VERIFICATION.md and fix all stale checkboxes across REQUIREMENTS.md and ROADMAP.md.

Purpose: Phase 4 is the only completed phase missing a verification artifact. Additionally, 9 requirement checkboxes and 4 roadmap phase checkboxes are stale (still unchecked despite the work being complete). This plan brings all planning artifacts into consistency.

Output: 04-VERIFICATION.md created; REQUIREMENTS.md and ROADMAP.md updated with correct checkbox and progress states.
</objective>

<execution_context>
@/Users/shivamsinghal/projects/bandwidth-monitor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shivamsinghal/projects/bandwidth-monitor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-popover-shell-and-interface-views/04-01-SUMMARY.md
@.planning/phases/04-popover-shell-and-interface-views/04-02-SUMMARY.md
@.planning/phases/04-popover-shell-and-interface-views/04-CONTEXT.md
@.planning/phases/04-popover-shell-and-interface-views/04-01-PLAN.md
@.planning/phases/04-popover-shell-and-interface-views/04-02-PLAN.md
@.planning/phases/05-historical-charts-statistics-and-settings/05-VERIFICATION.md
@.planning/phases/06-fix-polling-interval-sync/06-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 4 VERIFICATION.md</name>
  <files>.planning/phases/04-popover-shell-and-interface-views/04-VERIFICATION.md</files>
  <read_first>
    .planning/phases/04-popover-shell-and-interface-views/04-01-SUMMARY.md,
    .planning/phases/04-popover-shell-and-interface-views/04-02-SUMMARY.md,
    .planning/phases/04-popover-shell-and-interface-views/04-01-PLAN.md,
    .planning/phases/04-popover-shell-and-interface-views/04-02-PLAN.md,
    .planning/phases/04-popover-shell-and-interface-views/04-CONTEXT.md,
    .planning/phases/05-historical-charts-statistics-and-settings/05-VERIFICATION.md,
    .planning/phases/06-fix-polling-interval-sync/06-VERIFICATION.md,
    BandwidthMonitor/Popover/PopoverContentView.swift,
    BandwidthMonitor/Popover/PopoverTab.swift,
    BandwidthMonitor/Popover/AggregateHeaderView.swift,
    BandwidthMonitor/Popover/InterfaceRowView.swift,
    BandwidthMonitor/Popover/MetricsView.swift,
    BandwidthMonitor/MenuBar/StatusBarController.swift,
    BandwidthMonitorTests/PopoverTests.swift
  </read_first>
  <action>
Create `.planning/phases/04-popover-shell-and-interface-views/04-VERIFICATION.md` following the exact format established by 05-VERIFICATION.md and 06-VERIFICATION.md.

**Frontmatter fields (YAML between triple-dash lines):**
- phase: 04-popover-shell-and-interface-views
- verified: (current ISO 8601 timestamp when writing the file)
- status: human_needed
- score: (N/N automated must-haves verified -- fill in after counting)
- re_verification: false
- human_verification: array of 3 items:
  1. test: "Popover opens on left-click and shows live per-interface bandwidth data" / expected: "Left-clicking the status item opens a ~400x550 popover with a segmented Metrics/Preferences tab switcher. Metrics tab shows aggregate upload/download header with arrow SF Symbols and per-interface rows with SF Symbol icons, display names, and live-updating speeds." / why_human: "NSPopover rendering and real-time @Observable data flow require a running app."
  2. test: "Right-click context menu with Metrics, Preferences, About, Quit items" / expected: "Right-clicking the status item shows an NSMenu with 4 items: Metrics, Preferences, separator, About Bandwidth Monitor, Quit Bandwidth Monitor. Clicking Metrics/Preferences opens popover to that tab. Clicking Quit terminates the app." / why_human: "NSMenu behavior and tab navigation from context menu items require a running app."
  3. test: "Dark and light mode adaptation" / expected: "Toggling system appearance causes the popover to automatically adapt colors. No custom hex colors -- all semantic SwiftUI colors." / why_human: "Color rendering under different system appearances requires visual inspection."

**Document body structure (match 05-VERIFICATION.md format exactly):**

1. **Title:** `# Phase 4: Popover Shell and Interface Views Verification Report`

2. **Header block:** Phase Goal (from ROADMAP: "Users can click the menu bar item to see a popover window with per-interface bandwidth details, styled correctly for their system appearance"), Verified timestamp, Status: human_needed, Re-verification: No

3. **Goal Achievement > Observable Truths** section with two sub-tables:

   **Plan 01 Truths** -- verify each against source files by reading them:
   | # | Truth | Status | Evidence |
   - PopoverTab enum defines .metrics and .preferences cases with raw string values -- verify in PopoverTab.swift
   - sfSymbolName returns correct SF Symbols for wifi, ethernet, VPN/utun, other -- verify in PopoverTab.swift
   - PopoverContentView renders a segmented Picker switching tabs -- verify in PopoverContentView.swift
   - AggregateHeaderView shows upload/download with arrow SF Symbols -- verify in AggregateHeaderView.swift
   - InterfaceRowView shows SF Symbol + name + speeds -- verify in InterfaceRowView.swift
   - MetricsView composes AggregateHeaderView + Divider + ScrollView of InterfaceRowView -- verify in MetricsView.swift
   - All views use only semantic SwiftUI colors -- verify by grepping all Popover/*.swift files for hex colors (should find none)

   **Plan 02 Truths** -- verify each against source files:
   - Left-click opens NSPopover -- verify sendAction(on:), togglePopover in StatusBarController.swift
   - Left-click again while open closes it -- verify popover.isShown check in togglePopover
   - Right-click opens NSMenu with correct items -- verify buildContextMenu in StatusBarController.swift
   - Clicking Metrics in context menu opens popover on Metrics tab -- verify showMetrics method
   - Clicking Preferences opens popover on Preferences tab -- verify showPreferences method
   - Clicking Quit terminates the app -- verify NSApplication.terminate in context menu
   - Popover shows live-updating data from NetworkMonitor -- verify NSHostingController with PopoverContentView accepting networkMonitor
   - Popover renders in dark/light mode -- semantic colors only, verified by absence of custom hex

   For each truth, set Status to VERIFIED and provide specific evidence (file name, line numbers, code snippets found). Use the actual line numbers and content from reading the source files.

4. **Required Artifacts** section -- verify each file exists and contains expected content:
   - BandwidthMonitor/Popover/PopoverTab.swift -- contains `enum PopoverTab` and `func sfSymbolName`
   - BandwidthMonitor/Popover/PopoverContentView.swift -- contains `struct PopoverContentView: View`, `.frame(width: 400, height: 550)` (note: Phase 5 updated frame to 550 height)
   - BandwidthMonitor/Popover/AggregateHeaderView.swift -- contains `struct AggregateHeaderView: View`
   - BandwidthMonitor/Popover/InterfaceRowView.swift -- contains `struct InterfaceRowView: View`
   - BandwidthMonitor/Popover/MetricsView.swift -- contains `struct MetricsView: View`
   - BandwidthMonitor/MenuBar/StatusBarController.swift -- contains `NSPopover`, `sendAction(on:`, `NSMenuDelegate`
   - BandwidthMonitorTests/PopoverTests.swift -- contains `class PopoverTests: XCTestCase`

5. **Key Link Verification** section:
   - StatusBarController -> PopoverContentView via NSHostingController
   - StatusBarController -> NSPopover via popover.show(relativeTo:)
   - StatusBarController -> NSMenu via dynamic statusItem.menu assignment + nil-out in menuDidClose
   - PopoverContentView -> NetworkMonitor via let networkMonitor property (or popoverState pattern if Phase 5 changed it)
   - MetricsView -> NetworkMonitor via direct @Observable observation
   - InterfaceRowView -> SpeedFormatter via format(bytesPerSecond:) calls

6. **Behavioral Spot-Checks** section:
   - Build succeeds: run `xcodebuild build -scheme BandwidthMonitor -destination 'platform=macOS' -quiet` and report result
   - PopoverTests pass: run `xcodebuild test -scheme BandwidthMonitor -destination 'platform=macOS' -only-testing:BandwidthMonitorTests/PopoverTests -quiet` and report result (should be 9+ tests, 0 failures)

7. **Requirements Coverage** section:
   | Requirement | Source Plan | Description | Status | Evidence |
   - POP-01 | 04-01, 04-02 | Clicking menu bar opens ~400x500 popover | SATISFIED | PopoverContentView .frame(width: 400, height: 550), NSPopover contentSize NSSize(width: 400, height: 550)
   - POP-03 | 04-01, 04-02 | Per-interface breakdown with individual stats | SATISFIED | MetricsView with AggregateHeaderView + ScrollView of InterfaceRowView rows
   - POP-05 | 04-01 | Dark mode and light mode automatic support | SATISFIED | Semantic SwiftUI colors only (.primary, .secondary, Color.accentColor, Color(.separatorColor)) across all Popover views; zero custom hex values
   - POP-06 | 04-02 | Quit button | SATISFIED | "Quit Bandwidth Monitor" item in right-click context menu with action NSApplication.terminate (per D-12 from CONTEXT.md, quit is via context menu not inside popover)

8. **Anti-Patterns Found** section: Scan Popover/*.swift and StatusBarController.swift for TODO/FIXME/HACK/placeholder comments. Note: PreferencesPlaceholderView.swift was deleted by Phase 5, so it is not a remaining stub.

9. **Human Verification Required** section (3 items matching the frontmatter human_verification entries):
   - Popover opens on left-click with live data
   - Right-click context menu works correctly
   - Dark/light mode adaptation

10. **Gaps Summary** section: State whether any gaps were found.

11. Footer with verified timestamp and verifier line.

**IMPORTANT:** When writing evidence, use ACTUAL line numbers and code content from reading the source files. Do not fabricate line numbers. Read each file and cite what you find.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-popover-shell-and-interface-views/04-VERIFICATION.md && grep -c "VERIFIED" .planning/phases/04-popover-shell-and-interface-views/04-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/04-popover-shell-and-interface-views/04-VERIFICATION.md exists
    - File contains frontmatter with `phase: 04-popover-shell-and-interface-views`
    - File contains `status: human_needed`
    - File contains `# Phase 4: Popover Shell and Interface Views Verification Report`
    - File contains Observable Truths table with VERIFIED entries for all Plan 01 and Plan 02 truths
    - File contains Required Artifacts table with VERIFIED entries for all 7 artifacts
    - File contains Key Link Verification table with WIRED/VERIFIED entries
    - File contains Requirements Coverage table with SATISFIED entries for POP-01, POP-03, POP-05, POP-06
    - File contains Behavioral Spot-Checks with build and test results
    - File contains Human Verification Required section with 3 items
    - File contains Gaps Summary section
    - grep -c "VERIFIED" returns at least 15 (truths + artifacts)
    - grep -c "SATISFIED" returns exactly 4 (POP-01, POP-03, POP-05, POP-06)
    - grep "POP-01" returns a line containing SATISFIED
    - grep "POP-06" returns a line containing SATISFIED
  </acceptance_criteria>
  <done>Phase 4 VERIFICATION.md exists with all truths verified against actual source code, all 4 requirements (POP-01, POP-03, POP-05, POP-06) marked SATISFIED with evidence, and human verification items identified</done>
</task>

<task type="auto">
  <name>Task 2: Fix stale checkboxes in REQUIREMENTS.md and ROADMAP.md</name>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md
  </files>
  <read_first>
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md,
    .planning/phases/04-popover-shell-and-interface-views/04-02-SUMMARY.md,
    .planning/phases/06-fix-polling-interval-sync/06-01-SUMMARY.md
  </read_first>
  <action>
**File: .planning/REQUIREMENTS.md**

Make the following exact checkbox changes. Each change corrects a stale `[ ]` to `[x]` for a requirement that has been implemented and verified.

1. Line containing `**BAR-01**`: Change `- [ ]` to `- [x]` (Phase 2 complete, SpeedTextBuilder displays up/down speeds in menu bar)
2. Line containing `**BAR-02**`: Change `- [ ]` to `- [x]` (Phase 5 PreferencesView has unit picker)
3. Line containing `**BAR-03**`: Change `- [ ]` to `- [x]` (Phase 5 PreferencesView has display mode picker)
4. Line containing `**BAR-04**`: Change `- [ ]` to `- [x]` (Phase 2 SpeedTextBuilder uses monospacedDigit for fixed-width)
5. Line containing `**POP-01**`: Change `- [ ]` to `- [x]` (Phase 4 PopoverContentView .frame(width: 400, height: 550))
6. Line containing `**POP-03**`: Change `- [ ]` to `- [x]` (Phase 4 MetricsView with per-interface InterfaceRowView)
7. Line containing `**POP-05**`: Change `- [ ]` to `- [x]` (Phase 4 semantic colors only)
8. Line containing `**POP-06**`: Change `- [ ]` to `- [x]` (Phase 4 context menu Quit item, per D-12)
9. Line containing `**SYS-01**`: Change `- [ ]` to `- [x]` (Phase 2 SMAppService.mainApp.register)

Update the **Traceability** table:
- BAR-01 row: Change Status from `Pending` to `Complete`
- BAR-02 row: Change Status from `Pending` to `Complete`
- BAR-03 row: Change Status from `Pending` to `Complete`
- BAR-04 row: Change Status from `Pending` to `Complete`
- POP-01 row: Change Status from `Pending` to `Complete`
- POP-03 row: Change Status from `Pending` to `Complete`
- POP-05 row: Change Status from `Pending` to `Complete`
- POP-06 row: Change Status from `Pending` to `Complete`
- SYS-01 row: Change Status from `Pending` to `Complete`

Update the **Coverage** summary at the bottom. Recalculate: Total v1 requirements = 18. Previously checked: MON-01 through MON-06 (6) + POP-02, POP-04, SYS-02 (3) = 9 checked. After fixing 9 stale checkboxes (BAR-01 through BAR-04, POP-01, POP-03, POP-05, POP-06, SYS-01), all 18 are satisfied. Update the coverage block to:
- `- v1 requirements: 18 total`
- `- Mapped to phases: 18`
- `- Satisfied: 18 (checked)`
- `- Pending: 0`
- `- Unmapped: 0`

Remove or update the note about "6 gap closure phases + 5 stale checkboxes to fix" since all are now resolved.

Update the "Last updated" line at the bottom to the current date.

**File: .planning/ROADMAP.md**

Make the following exact checkbox changes in the phase list near the top:

1. Line `- [ ] **Phase 1: Core Monitoring Engine**`: Change `- [ ]` to `- [x]`
2. Line `- [ ] **Phase 2: Menu Bar Display**`: Change `- [ ]` to `- [x]`
3. Line `- [ ] **Phase 3: Data Persistence and Aggregation**`: Change `- [ ]` to `- [x]`
4. Line `- [ ] **Phase 6: Fix Polling Interval Sync**`: Change `- [ ]` to `- [x]`
5. Line `- [ ] **Phase 7: Verification and Artifact Cleanup**`: Leave as `- [ ]` (this phase is currently in progress, not yet complete)

Update the **Progress** table:
- Phase 6 row: Change `0/1` to `1/1`, change `Planned` to `Complete`, add date `2026-03-24` in Completed column
- Phase 7 row: Change `0/0` to `0/1`, keep status as `Planned`

Update Phase 7 detail section:
- Change `**Plans:** 0 plans` to `**Plans:** 1 plan`
- Add plan listing after the Plans line:
  `- [ ] 07-01-PLAN.md -- Create Phase 4 VERIFICATION.md and fix stale requirement/roadmap checkboxes`

Update Phase 4 detail section:
- Line `- [ ] 04-02-PLAN.md`: Change to `- [x] 04-02-PLAN.md` (Phase 4 Plan 02 was completed per 04-02-SUMMARY.md)
  </action>
  <verify>
    <automated>grep -c "\[x\]" .planning/REQUIREMENTS.md && grep -c "\[x\]" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md: `grep "\[x\] \*\*BAR-01\*\*"` returns a match
    - .planning/REQUIREMENTS.md: `grep "\[x\] \*\*BAR-02\*\*"` returns a match
    - .planning/REQUIREMENTS.md: `grep "\[x\] \*\*BAR-03\*\*"` returns a match
    - .planning/REQUIREMENTS.md: `grep "\[x\] \*\*BAR-04\*\*"` returns a match
    - .planning/REQUIREMENTS.md: `grep "\[x\] \*\*POP-01\*\*"` returns a match
    - .planning/REQUIREMENTS.md: `grep "\[x\] \*\*POP-03\*\*"` returns a match
    - .planning/REQUIREMENTS.md: `grep "\[x\] \*\*POP-05\*\*"` returns a match
    - .planning/REQUIREMENTS.md: `grep "\[x\] \*\*POP-06\*\*"` returns a match
    - .planning/REQUIREMENTS.md: `grep "\[x\] \*\*SYS-01\*\*"` returns a match
    - .planning/REQUIREMENTS.md: `grep "Satisfied: 18"` returns a match
    - .planning/REQUIREMENTS.md: All traceability table rows show `Complete` status
    - .planning/ROADMAP.md: `grep "\[x\] \*\*Phase 1:"` returns a match
    - .planning/ROADMAP.md: `grep "\[x\] \*\*Phase 2:"` returns a match
    - .planning/ROADMAP.md: `grep "\[x\] \*\*Phase 3:"` returns a match
    - .planning/ROADMAP.md: `grep "\[x\] \*\*Phase 6:"` returns a match
    - .planning/ROADMAP.md: Phase 6 progress row contains `1/1` and `Complete` and `2026-03-24`
    - .planning/ROADMAP.md: Phase 7 progress row contains `0/1`
    - .planning/ROADMAP.md: Phase 7 section contains `07-01-PLAN.md`
    - .planning/ROADMAP.md: `grep "\[x\] 04-02-PLAN.md"` returns a match
  </acceptance_criteria>
  <done>All 9 stale REQUIREMENTS.md checkboxes fixed (18/18 satisfied), all traceability rows show Complete, ROADMAP.md phase checkboxes corrected for phases 1-3 and 6, progress table updated with Phase 6 complete and Phase 7 plan count, Phase 4 Plan 02 checkbox corrected</done>
</task>

</tasks>

<verification>
- `test -f .planning/phases/04-popover-shell-and-interface-views/04-VERIFICATION.md` succeeds (Phase 4 verification exists)
- `grep -c "SATISFIED" .planning/phases/04-popover-shell-and-interface-views/04-VERIFICATION.md` returns 4 (POP-01, POP-03, POP-05, POP-06)
- `grep -c "\[ \] \*\*[A-Z]" .planning/REQUIREMENTS.md` returns 0 (no stale v1 checkboxes)
- `grep "Satisfied: 18" .planning/REQUIREMENTS.md` matches (all v1 requirements satisfied)
- `grep "\[x\] \*\*Phase 1:" .planning/ROADMAP.md` matches
- `grep "\[x\] \*\*Phase 6:" .planning/ROADMAP.md` matches
- `grep "1/1.*Complete" .planning/ROADMAP.md` matches Phase 6 progress row
</verification>

<success_criteria>
- Phase 4 VERIFICATION.md created with all must_haves truths verified, all 4 requirements marked SATISFIED, and human verification items identified
- REQUIREMENTS.md has 18/18 v1 requirements checked, all traceability rows showing Complete
- ROADMAP.md has correct phase checkboxes (1-5 checked, 6 checked, 7 unchecked), correct progress table, Phase 7 plan listing
- All planning artifacts are internally consistent
</success_criteria>

<output>
After completion, create `.planning/phases/07-verification-and-artifact-cleanup/07-01-SUMMARY.md`
</output>
