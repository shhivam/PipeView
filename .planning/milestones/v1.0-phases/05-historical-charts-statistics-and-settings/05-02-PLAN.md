---
phase: 05-historical-charts-statistics-and-settings
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - BandwidthMonitor/Popover/PopoverTab.swift
  - BandwidthMonitor/Popover/PopoverContentView.swift
  - BandwidthMonitor/Popover/HistoryView.swift
  - BandwidthMonitor/Popover/HistoryChartView.swift
  - BandwidthMonitor/Popover/CumulativeStatsView.swift
  - BandwidthMonitor/Popover/StatCardView.swift
  - BandwidthMonitor/Logging/Loggers.swift
  - BandwidthMonitorTests/PopoverTests.swift
autonomous: true
requirements:
  - POP-02
  - POP-04

must_haves:
  truths:
    - "PopoverTab has a .history case with rawValue 'History'"
    - "PopoverContentView routes to HistoryView when .history tab is selected"
    - "Popover frame is 400x550 pixels per D-02"
    - "History tab shows a time range segmented control with 1H, 24H, 7D, 30D options per D-04"
    - "Chart displays grouped download/upload bars per time bucket per D-05, D-06"
    - "Chart shows interactive tooltip on hover/selection per D-08"
    - "Cumulative stats show Today, This Week, This Month cards below chart per D-11"
    - "Stats cards show combined total with per-direction breakdown per D-09"
    - "Empty state shows zero baseline with 'No data yet' overlay per D-12"
  artifacts:
    - path: "BandwidthMonitor/Popover/PopoverTab.swift"
      provides: "PopoverTab.history case"
      contains: "case history"
    - path: "BandwidthMonitor/Popover/HistoryView.swift"
      provides: "History tab container: time range picker + chart + stats"
      min_lines: 40
    - path: "BandwidthMonitor/Popover/HistoryChartView.swift"
      provides: "Swift Charts grouped BarMark chart with selection"
      min_lines: 50
    - path: "BandwidthMonitor/Popover/CumulativeStatsView.swift"
      provides: "Horizontal row of 3 StatCardView cards"
      min_lines: 20
    - path: "BandwidthMonitor/Popover/StatCardView.swift"
      provides: "Individual stat card with combined total and direction breakdown"
      min_lines: 20
    - path: "BandwidthMonitor/Popover/PopoverContentView.swift"
      provides: "Updated with .history tab routing and 550px height"
      contains: "case .history"
  key_links:
    - from: "BandwidthMonitor/Popover/HistoryView.swift"
      to: "BandwidthMonitor/Persistence/AppDatabase.swift"
      via: "fetchChartData and fetchCumulativeStats calls"
      pattern: "fetchChartData|fetchCumulativeStats"
    - from: "BandwidthMonitor/Popover/HistoryChartView.swift"
      to: "BandwidthMonitor/Shared/ChartDataPoint.swift"
      via: "Renders [ChartDataPoint] as BarMark chart"
      pattern: "ChartDataPoint"
    - from: "BandwidthMonitor/Popover/CumulativeStatsView.swift"
      to: "BandwidthMonitor/Shared/ByteFormatter.swift"
      via: "Formats byte totals for display"
      pattern: "ByteFormatter"
    - from: "BandwidthMonitor/Popover/PopoverContentView.swift"
      to: "BandwidthMonitor/Popover/HistoryView.swift"
      via: "Tab switch routes to HistoryView"
      pattern: "case .history.*HistoryView"
---

<objective>
Build the History tab with bar charts and cumulative statistics. This plan creates the visual experience: a time-range-switchable grouped bar chart showing download/upload data, with three stats cards (Today, This Week, This Month) below. It also updates the popover shell to include the new History tab.

Purpose: Delivers POP-02 (historical charts) and POP-04 (cumulative statistics) -- the core data visualization that lets users understand their bandwidth usage patterns over time.
Output: 4 new view files, updated PopoverTab and PopoverContentView, History logger category.
</objective>

<execution_context>
@/Users/shivamsinghal/projects/bandwidth-monitor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shivamsinghal/projects/bandwidth-monitor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-historical-charts-statistics-and-settings/05-CONTEXT.md
@.planning/phases/05-historical-charts-statistics-and-settings/05-RESEARCH.md
@.planning/phases/05-historical-charts-statistics-and-settings/05-01-SUMMARY.md

<interfaces>
<!-- Types created by Plan 01 that this plan depends on. -->

From BandwidthMonitor/Shared/ChartDataPoint.swift (Plan 01):
```swift
struct ChartDataPoint: Identifiable, Sendable {
    let id: UUID
    let timestamp: Date
    let totalBytesIn: Double
    let totalBytesOut: Double
    var totalBytes: Double { totalBytesIn + totalBytesOut }
}
```

From BandwidthMonitor/Shared/HistoryTimeRange.swift (Plan 01):
```swift
enum HistoryTimeRange: String, CaseIterable, Sendable {
    case oneHour = "1H"
    case twentyFourHours = "24H"
    case sevenDays = "7D"
    case thirtyDays = "30D"
    var tier: AggregationTier { ... }
    var timeInterval: TimeInterval { ... }
    var calendarUnit: Calendar.Component { ... }
}
```

From BandwidthMonitor/Shared/ByteFormatter.swift (Plan 01):
```swift
struct ByteFormatter: Sendable {
    func format(bytes: Double) -> String  // "1.2 GB", "45.3 MB"
}
```

From BandwidthMonitor/Persistence/AppDatabase.swift (Plan 01 additions):
```swift
extension AppDatabase {
    func fetchChartData(tier: AggregationTier, since: Date) throws -> [ChartDataPoint]
    func fetchCumulativeStats(since: Date) throws -> (totalIn: Double, totalOut: Double)
}
```

From BandwidthMonitor/Popover/PopoverContentView.swift (existing):
```swift
struct PopoverContentView: View {
    let networkMonitor: NetworkMonitor
    @Binding var selectedTab: PopoverTab
    // body: Picker + switch on selectedTab
    // .frame(width: 400, height: 500) -- needs update to 550
}
```

From BandwidthMonitor/Popover/PopoverTab.swift (existing):
```swift
enum PopoverTab: String, CaseIterable, Sendable {
    case metrics = "Metrics"
    case preferences = "Preferences"
    // needs .history = "History" case
}
```

From BandwidthMonitor/MenuBar/StatusBarController.swift (existing):
```swift
// popover.contentSize = NSSize(width: 400, height: 500) -- needs update to 550
// PopoverContentView needs appDatabase parameter for HistoryView
```

From BandwidthMonitor/MenuBar/SpeedFormatter.swift (existing):
```swift
struct SpeedFormatter: Sendable {
    func format(bytesPerSecond: Double, unit: UnitMode = .auto) -> String
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update popover shell and create History tab container</name>
  <files>
    BandwidthMonitor/Popover/PopoverTab.swift,
    BandwidthMonitor/Popover/PopoverContentView.swift,
    BandwidthMonitor/Popover/HistoryView.swift,
    BandwidthMonitor/Logging/Loggers.swift,
    BandwidthMonitor/MenuBar/StatusBarController.swift,
    BandwidthMonitor/AppDelegate.swift,
    BandwidthMonitorTests/PopoverTests.swift
  </files>
  <action>
    **Modify `PopoverTab.swift`** (per D-01):
    - Add `case history = "History"` between `metrics` and `preferences`. The order in CaseIterable determines the segmented control order: Metrics | History | Preferences.

    **Modify `PopoverContentView.swift`** (per D-01, D-02, D-03):
    - Add `let appDatabase: AppDatabase` parameter (needed by HistoryView for data queries).
    - Add `case .history: HistoryView(appDatabase: appDatabase)` to the switch statement.
    - Update `.frame(width: 400, height: 500)` to `.frame(width: 400, height: 550)` per D-02.

    **Modify `StatusBarController.swift`**:
    - Update `popover.contentSize = NSSize(width: 400, height: 500)` to `NSSize(width: 400, height: 550)` per D-02.
    - Add `private let appDatabase: AppDatabase` property.
    - Update `init` to accept `appDatabase: AppDatabase` parameter.
    - Pass `appDatabase` to `PopoverContentView` in both `popover` lazy var and `updatePopoverRootView()`.
    - Add "History" menu item to `buildContextMenu()` between Metrics and Preferences, with corresponding `@objc private func showHistory()` that sets `selectedTab = .history` and calls `showPopover()`.

    **Modify `AppDelegate.swift`**:
    - Pass `appDatabase` (the `database` local variable) to `StatusBarController` init. Handle the case where database init failed -- pass a fallback or make the parameter optional. Best approach: make `appDatabase` on StatusBarController optional (`AppDatabase?`), and make HistoryView handle nil gracefully (show empty state).

    **Create `HistoryView.swift`** (per D-03, D-04):
    - `struct HistoryView: View` with `let appDatabase: AppDatabase?`.
    - `@State private var selectedRange: HistoryTimeRange = .twentyFourHours` (sensible default).
    - `@State private var chartData: [ChartDataPoint] = []`
    - `@State private var todayStats: (totalIn: Double, totalOut: Double) = (0, 0)`
    - `@State private var weekStats: (totalIn: Double, totalOut: Double) = (0, 0)`
    - `@State private var monthStats: (totalIn: Double, totalOut: Double) = (0, 0)`
    - Layout (VStack): Time range picker (Picker with .segmented style, iterating HistoryTimeRange.allCases) -> HistoryChartView(dataPoints: chartData, timeRange: selectedRange) -> CumulativeStatsView(today: todayStats, week: weekStats, month: monthStats).
    - Padding: .horizontal(16), .top(8) for picker, chart gets flex space.
    - `.onChange(of: selectedRange)` triggers `loadChartData()`.
    - `.onAppear` triggers `loadChartData()` and `loadStats()`.
    - `loadChartData()`: guard let appDatabase, compute `since = Date.now.addingTimeInterval(-selectedRange.timeInterval)`, call `try appDatabase.fetchChartData(tier: selectedRange.tier, since: since)`.
    - `loadStats()`: guard let appDatabase. Compute today start: `Calendar.current.startOfDay(for: .now)`. Compute week start: use `Calendar.current.dateInterval(of: .weekOfYear, for: .now)?.start`. Compute month start: use `Calendar.current.dateInterval(of: .month, for: .now)?.start`. Per Pitfall 6 (UTC vs local time), these use local calendar dates automatically. Call fetchCumulativeStats for each.
    - If appDatabase is nil, show empty state.

    **Modify `Loggers.swift`**:
    - Add `static let history = Logger(subsystem: subsystem, category: "history")` for chart data loading.

    **Update `PopoverTests.swift`**:
    - Add `testPopoverTab_historyRawValue` verifying `PopoverTab.history.rawValue == "History"`.
    - Update `testPopoverTab_allCasesCount` to expect 3 (was 2).
  </action>
  <verify>
    <automated>xcodebuild test -project BandwidthMonitor.xcodeproj -scheme BandwidthMonitor -destination 'platform=macOS' -only-testing:BandwidthMonitorTests/PopoverTests 2>&1 | tail -20</automated>
  </verify>
  <done>PopoverTab has .history case. PopoverContentView routes to HistoryView and is 550px tall. StatusBarController passes appDatabase and has 550px popover. HistoryView loads chart data on range change and stats on appear. Context menu includes "History" option. PopoverTests pass with updated counts.</done>
</task>

<task type="auto">
  <name>Task 2: Create chart and stats card views</name>
  <files>
    BandwidthMonitor/Popover/HistoryChartView.swift,
    BandwidthMonitor/Popover/CumulativeStatsView.swift,
    BandwidthMonitor/Popover/StatCardView.swift
  </files>
  <action>
    **Create `HistoryChartView.swift`** (per D-05, D-06, D-07, D-08, D-12, POP-02):
    - `import Charts` and `import SwiftUI`.
    - `struct HistoryChartView: View` with `let dataPoints: [ChartDataPoint]`, `let timeRange: HistoryTimeRange`.
    - `@State private var selectedTimestamp: Date?` for interactive selection per D-08.
    - Body: `Chart { ForEach(dataPoints) { point in ... } }` with two `BarMark` per point:
      - Download bar: `BarMark(x: .value("Time", point.timestamp, unit: timeRange.calendarUnit), y: .value("Bytes", point.totalBytesIn))` with `.foregroundStyle(by: .value("Direction", "Download"))` and `.position(by: .value("Direction", "Download"))`.
      - Upload bar: Same pattern with `point.totalBytesOut` and "Upload" direction.
    - Color scale per D-06: `.chartForegroundStyleScale(["Download": Color.accentColor, "Upload": Color.secondary.opacity(0.7)])`. Download gets accent color (prominent), upload gets secondary with slight opacity.
    - Selection per D-08: `.chartXSelection(value: $selectedTimestamp)`. Add a `RuleMark` when selectedTimestamp is non-nil, with `.annotation(position: .top, alignment: .center)` showing a tooltip VStack with formatted timestamp and byte values using SpeedFormatter for speed or ByteFormatter for totals.
    - Tooltip: Find the closest ChartDataPoint to selectedTimestamp. Display: date/time formatted appropriately for the range, download bytes formatted, upload bytes formatted.
    - Empty state per D-12: If `dataPoints.isEmpty`, overlay the chart area with `Text("No data yet").foregroundStyle(.secondary).font(.callout)` centered. The chart should still render (shows axes with zero baseline).
    - Chart height: `.frame(height: 200)` (hero element per D-03, leave room for stats below).
    - Y-axis: Let Swift Charts auto-scale. X-axis: Let Swift Charts auto-format dates. If the auto-formatting is poor, add `.chartXAxis { AxisMarks(values: .automatic) { value in AxisValueLabel() } }` -- but start without custom axis labels.

    **Create `StatCardView.swift`** (per D-09, D-10, D-11, POP-04):
    - `struct StatCardView: View` with `let title: String`, `let totalIn: Double`, `let totalOut: Double`.
    - Computed `var totalBytes: Double { totalIn + totalOut }`.
    - Private `let formatter = ByteFormatter()`.
    - Layout (VStack, alignment: .leading, spacing: 4):
      - `Text(title).font(.caption).foregroundStyle(.secondary)` (label: "Today", "This Week", "This Month")
      - `Text(formatter.format(bytes: totalBytes)).font(.title3).fontWeight(.semibold)` (primary combined number per D-09)
      - `HStack(spacing: 8)` with:
        - `Text("\u{2193} \(formatter.format(bytes: totalIn))").font(.caption2).foregroundStyle(.secondary)` (down-arrow download)
        - `Text("\u{2191} \(formatter.format(bytes: totalOut))").font(.caption2).foregroundStyle(.secondary)` (up-arrow upload)
    - Wrap in a RoundedRectangle background: `.padding(8).background(.quaternary.opacity(0.5), in: RoundedRectangle(cornerRadius: 8))`. Use semantic color for dark/light mode support.
    - Make the card `frame(maxWidth: .infinity)` to share space equally in HStack.

    **Create `CumulativeStatsView.swift`** (per D-11, POP-04):
    - `struct CumulativeStatsView: View` with `let today: (totalIn: Double, totalOut: Double)`, `let week: (totalIn: Double, totalOut: Double)`, `let month: (totalIn: Double, totalOut: Double)`.
    - Layout: `HStack(spacing: 8)` containing:
      - `StatCardView(title: "Today", totalIn: today.totalIn, totalOut: today.totalOut)`
      - `StatCardView(title: "This Week", totalIn: week.totalIn, totalOut: week.totalOut)`
      - `StatCardView(title: "This Month", totalIn: month.totalIn, totalOut: month.totalOut)`
    - Padding: `.padding(.horizontal, 16).padding(.bottom, 16)`.
  </action>
  <verify>
    <automated>xcodebuild build -project BandwidthMonitor.xcodeproj -scheme BandwidthMonitor -destination 'platform=macOS' 2>&1 | tail -10</automated>
  </verify>
  <done>HistoryChartView renders grouped download/upload bars with selection tooltip. StatCardView shows combined total with per-direction breakdown. CumulativeStatsView arranges 3 cards horizontally. Empty state shows "No data yet" overlay. Project builds without errors.</done>
</task>

</tasks>

<verification>
1. Build succeeds: `xcodebuild build -project BandwidthMonitor.xcodeproj -scheme BandwidthMonitor -destination 'platform=macOS' 2>&1 | tail -10`
2. All tests pass: `xcodebuild test -project BandwidthMonitor.xcodeproj -scheme BandwidthMonitor -destination 'platform=macOS' 2>&1 | tail -30`
3. PopoverTab has 3 cases (Metrics, History, Preferences)
4. PopoverContentView frame is 400x550
</verification>

<success_criteria>
1. PopoverTab.history exists with rawValue "History"
2. PopoverContentView routes to HistoryView for .history tab with 550px height
3. HistoryView loads chart data from AppDatabase and shows time range picker
4. HistoryChartView renders grouped BarMark bars with download (accent) and upload (secondary) colors
5. Chart supports interactive selection with tooltip showing formatted values
6. CumulativeStatsView shows Today, This Week, This Month cards with combined totals
7. Empty state displays "No data yet" when no chart data exists
8. StatusBarController popover size is 550px and context menu has "History" item
9. All existing tests plus new PopoverTests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-historical-charts-statistics-and-settings/05-02-SUMMARY.md`
</output>
