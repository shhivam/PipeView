# Deferred Items - Phase 03

## Out-of-Scope Discoveries

1. **Missing .gitignore** - The project has no `.gitignore` file. `xcuserdata/` directories (user-specific Xcode state) are untracked but should be explicitly ignored. Consider adding a standard Swift/Xcode `.gitignore` in a future chore task.
