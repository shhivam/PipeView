import SwiftUI

/// Placeholder for the Preferences tab (per D-05).
/// Will be replaced with actual preferences controls in Phase 5.
struct PreferencesPlaceholderView: View {
    var body: some View {
        VStack {
            Spacer()
            Text("Coming in a future update.")
                .font(.body)
                .foregroundStyle(.secondary)
            Spacer()
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
